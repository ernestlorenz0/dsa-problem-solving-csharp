# C# Data Structures & Algorithms Evolution 🚀

My practical journey mastering Data Structures and Algorithms and Problem Solving from scratch using C# scripts (.csx). 

## 📊 Progress Tracker
- **Total Problems Solved:** 20 / 30 (Day 3)
- **Focus:** Brute-force logic first, optimizing later.

## 🗺️ Repository Map

### 📁 01_Arrays
* **Day 1: Linear Traversal Drills**
  - [x] ConditionalSum (`O(N)` Time / `O(1)` Space)
  - [x] CountEvens (`O(N)` Time / `O(1)` Space)
  - [x] CountPositives (`O(N)` Time / `O(1)` Space)
  - [x] FindMaximum (`O(N)` Time / `O(1)` Space)
  - [x] FindMinimum (`O(N)` Time / `O(1)` Space)
  - [x] FirstIndexSearch (`O(N)` Time / `O(1)` Space)
  - [x] FrequencyCount (`O(N)` Time / `O(1)` Space)
  - [x] MaxDifference (`O(N)` Time / `O(1)` Space)
  - [x] SearchElement (`O(N)` Time / `O(1)` Space)
  - [x] SumOfElements (`O(N)` Time / `O(1)` Space)

### 📁 02_Lists
* **Day 2: Linear Traversal Drills (Lists)**
  - [x] SumOfEvens (`O(N)` Time / `O(1)` Space)
  - [x] SumOfOdds (`O(N)` Time / `O(1)` Space)
  - [x] CountGreaterThanX (`O(N)` Time / `O(1)` Space)
  - [x] CountLessThanX (`O(N)` Time / `O(1)` Space)
  - [x] FindFirstPositive (`O(N)` Time / `O(1)` Space)
  - [x] FindLastNegative (`O(N)` Time / `O(1)` Space)
  - [x] CheckAllPositive (`O(N)` Time / `O(1)` Space)
  - [x] CheckAnyNegative (`O(N)` Time / `O(1)` Space)
  - [x] FrequencyOfEvens (`O(N)` Time / `O(1)` Space)
  - [x] ConditionalMax (`O(N)` Time / `O(1)` Space)

### 📁 03_Mixed_Linear_Traversal
* **Day 3: State Tracking & Early Exits (Arrays/Lists)**
  - [ ] CountZeros (`O(N)` Time / `O(1)` Space)
  - [ ] SumUntilNegative (`O(N)` Time / `O(1)` Space)
  - [ ] FindIndexOfMax (`O(N)` Time / `O(1)` Space)
  - [ ] FindIndexOfMin (`O(N)` Time / `O(1)` Space)
  - [x] CheckIncreasing (`O(N)` Time / `O(1)` Space)
  - [x] CheckDecreasing (`O(N)` Time / `O(1)` Space)
  - [ ] CountEqualToX (`O(N)` Time / `O(1)` Space)
  - [ ] SumOfMultiples (`O(N)` Time / `O(1)` Space)
  - [ ] FindFirstEven (`O(N)` Time / `O(1)` Space)
  - [ ] FindLastOdd (`O(N)` Time / `O(1)` Space)